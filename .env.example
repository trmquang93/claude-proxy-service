# JWT Secret for authentication tokens
# Generate a secure random string for production
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server port (Railway will provide this automatically in production)
PORT=3000

# Claude OAuth Client ID
# This is the official Anthropic OAuth client ID
CLAUDE_CLIENT_ID=9d1c250a-e61b-44d9-88ed-5944d1962f5e

# PostgreSQL Database URL
# Railway will provide this automatically when you add the PostgreSQL plugin
# For local development, set up a local PostgreSQL instance and update this URL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/claude_proxy

# Node Environment
NODE_ENV=development
